import { createContext } from "react";

export const QuizContext = createContext() ;


// default export --> exports only one function, in the component which is same as the file name
//and named export --> when we want to export multiple functions that are present in the component
